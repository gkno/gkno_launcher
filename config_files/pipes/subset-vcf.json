{
  "id" : "subset-vcf",
  "configuration type" : "pipeline",
  "description" : "Subset a VCF file to only include variants polymorphic in the specified samples. Subsetting performed using vt and the VCF is indexed with tabix.",
  "categories" : ["Variant-discovery"],

  "pipeline tasks" : [
    {
      "task" : "subset",
      "tool" : "vt-subset"
    },
    {
      "task" : "index",
      "tool" : "vcf-index"
    }
  ],

  "import arguments" : "subset",

  "arguments" : {
  },

  "unique graph nodes" : [
    {
      "id" : "vcf",
      "task" : "subset",
      "task argument" : "--in"
    },
    {
      "id" : "samples",
      "task" : "subset",
      "task argument" : "--samples-file"
    }
  ],

  "shared graph nodes" : [
    {
      "id" : "out",
      "arguments sharing node" : [
        {
          "task" : "subset",
          "task argument" : "--out"
        },
        {
          "task" : "index",
          "task argument" : "--in"
        }
      ]
    }
  ],

  "parameter sets" : [
    {
      "id" : "default",
      "description" : "Default parameter set requires user to input all input filenames.",
      "data" : []
    },

    {
      "id" : "test",
      "description" : "Test parameter set for the test harness.",
      "data" : [
        {
          "id" : "vcf",
          "node" : "vcf",
          "values" : ["$(RESOURCES)/tutorial/current/mutated_genome.vcf"]
        },
        {
          "id" : "samples",
          "node" : "samples",
          "values" : ["$(RESOURCES)/tutorial/current/subset-samples.txt"]
        }
      ]
    }
  ]
}
