{
  "id" : "snpeff-databases",
  "description" : "Write the available snpeff databases to the output file.",
  "categories" : ["Annotation"],
  "tools" : ["snpEff"],
  "path" : "snpEff",
  "executable" : "snpEff.jar",
  "precommand" : "java -Xmx4g -jar",
  "modifier" : "databases",
  "help" : "--help|-h",
  "arguments" : {
    "inputs" : [
    ],

    "outputs" : [
      {
        "description" : "A list of all available snpeff databases.",
        "long form argument" : "--out",
        "short form argument" : "-o",
        "command line argument" : "-out",
        "required" : true,
        "data type" : "string",
        "extensions" : ["no extension"],
        "modify argument name on command line" : "stdout"
      }
    ]
  },

  "parameter sets" : [
    {
      "ID" : "default",
      "description" : "Default parameter set requires the users input for all files.",
      "nodes" : []
    },

    {
      "ID" : "test",
      "description" : "Parameter set for use with the test-harness.",
      "nodes" : [
        {
          "ID" : "genome",
          "argument" : "--genome",
          "values" : ["ADWO01"]
        }
      ]
    }
  ]
}
