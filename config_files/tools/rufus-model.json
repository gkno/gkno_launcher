{
  "id" : "rufus-model",
  "description" : "Performs an analysis of the k-mer frequency histogram, outputting cutoffs for the maximum error rate and minimum coverage for a given sample.",
  "experimental" : true,
  "categories" : ["Kmer-processing", "Rufus", "Marthlab-software"],
  "tools" : ["Rufus"],
  "path" : "Rufus/bin",
  "executable" : "ModelDist2",
  "help" : "--help|-h",
  "arguments" : {
    "inputs" : [
      {
        "description" : "Input histogram of hash counts.",
        "long form argument" : "--in",
        "short form argument" : "-i",
        "command line argument" : "--histogram",
        "required" : true,
        "data type" : "string",
        "extensions" : [".histo"],
        "modify argument name on command line" : "hide"
      }
    ],

    "outputs" : [
      {
        "description" : "Output dist file.",
        "long form argument" : "--out-dist",
        "short form argument" : "-od",
        "command line argument" : "--output-dist",
        "required" : true,
        "data type" : "string",
        "extensions" : [".dist"],
        "modify argument name on command line" : "omit",
        "hide in help" : true,
        "construct filename" : {
          "method": "from tool argument",
          "use argument" : "--in",
          "modify extension" : "append"
        }
      },
      {
        "description" : "The model file contains information on how to fit normal distributions to the k-mer frequency histogram as well as suggested cut-off values for other Rufus tools.",
        "long form argument" : "--out-model",
        "short form argument" : "-om",
        "command line argument" : "--output-model",
        "required" : true,
        "data type" : "string",
        "extensions" : [".model"],
        "modify argument name on command line" : "omit",
        "hide in help" : true,
        "construct filename" : {
          "method": "from tool argument",
          "use argument" : "--in",
          "modify extension" : "append"
        }
      },
      {
        "description" : "Output prob file.",
        "long form argument" : "--out-prob",
        "short form argument" : "-pr",
        "command line argument" : "--output-prob",
        "required" : true,
        "data type" : "string",
        "extensions" : [".prob"],
        "modify argument name on command line" : "omit",
        "hide in help" : true,
        "construct filename" : {
          "method": "from tool argument",
          "use argument" : "--in",
          "modify extension" : "append"
        }
      }
    ],

    "options" : [
      {
        "description" : "Length of k-mer.",
        "long form argument" : "--k-mer",
        "short form argument" : "-k",
        "command line argument" : "--k-mer",
        "required" : true,
        "data type" : "integer",
        "extensions" : ["no extension"],
        "modify argument name on command line" : "hide"
      },
      {
        "description" : "Number of threads.",
        "long form argument" : "--threads",
        "short form argument" : "-t",
        "command line argument" : "--threads",
        "required" : true,
        "data type" : "integer",
        "extensions" : ["no extension"],
        "modify argument name on command line" : "hide"
      }
    ]
  },

  "argument order" : [
    "--in",
    "--k-mer",
    "--threads",
    "--out-dist",
    "--out-model",
    "--out-prob"
  ],

  "parameter sets" : [
    {
      "ID" : "default",
      "description" : "Default parameter set requires the users input for all files.",
      "nodes" : [
        {
          "ID" : "threads",
          "argument" : "--threads",
          "values" : [1]
        }
      ]
    },

    {
      "ID" : "test",
      "description" : "Parameter set for use with the test-harness.",
      "nodes" : [
      ]
    }
  ]
}
